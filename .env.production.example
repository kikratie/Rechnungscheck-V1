# =============================================================
# Ki2Go Accounting — Production Environment
# =============================================================
# Copy this file to .env.production and fill in real values.
# NEVER commit .env.production to git!

# --- App ---
NODE_ENV=production
PORT=3001
FRONTEND_URL=https://ki2go.example.com

# --- Caddy (Domain + SSL) ---
DOMAIN=ki2go.example.com
ACME_EMAIL=admin@example.com

# --- Database ---
POSTGRES_USER=buchungsai
POSTGRES_PASSWORD=CHANGE_ME_STRONG_DB_PASSWORD
POSTGRES_DB=buchungsai
# Note: DATABASE_URL is auto-constructed in docker-compose.prod.yml

# --- Redis ---
REDIS_PASSWORD=CHANGE_ME_STRONG_REDIS_PASSWORD

# --- Auth ---
# Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_MINIMUM_32_CHARACTERS_RANDOM_STRING
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# --- S3 / MinIO ---
S3_ACCESS_KEY=CHANGE_ME_MINIO_ACCESS_KEY
S3_SECRET_KEY=CHANGE_ME_MINIO_SECRET_KEY
S3_BUCKET=buchungsai
S3_REGION=eu-central-1

# --- OpenAI (optional, for OCR) ---
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o

# --- Email / SMTP (optional) ---
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=noreply@example.com
# SMTP_PASS=smtp_password
# SMTP_FROM_NAME=Ki2Go Accounting
# SMTP_FROM_EMAIL=noreply@example.com
# SMTP_SECURE=false

# --- Email Connector Encryption (optional) ---
# Generate with: openssl rand -hex 32
# ENCRYPTION_KEY=64_char_hex_string

# --- Rate Limiting ---
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
